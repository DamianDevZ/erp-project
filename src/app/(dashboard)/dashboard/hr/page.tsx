import { HRDashboard } from '@/features/dashboard';

/**
 * HR Department Dashboard page.
 * Shows employee stats, pending leaves, document compliance, and onboarding status.
 */
export default function HRDashboardPage() {
  return <HRDashboard />;
}
